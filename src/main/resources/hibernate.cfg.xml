<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Database connection -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/task_manager</property>
        <property name="hibernate.connection.username">soham</property>
        <property name="hibernate.connection.password"/>

        <!-- Dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Schema handling -->
        <property name="hibernate.hbm2ddl.auto">validate</property>

        <!-- SQL logging -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Entity classes -->
        <mapping class="me.isoham.taskmanager.model.User"/>
        <mapping class="me.isoham.taskmanager.model.Task"/>

    </session-factory>
</hibernate-configuration>